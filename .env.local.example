# This site's own public deployment URL (no trailing slash), e.g.
# https://cleano.services. Used to build absolute links (OG tags, quote
# confirmation emails) that shouldn't be derived from request headers.
NEXT_PUBLIC_APP_URL=

# The Cleano Ops app, where the "Get a Quote" flow ultimately hands off leads
# and where the customer booking portal lives.
NEXT_PUBLIC_OPS_APP_URL=https://ops.cleano.services

# Supabase (Project Settings -> API Keys) - a SEPARATE Supabase project from
# Cleano Ops, so a leaked marketing-site key can't touch operational data.
# Server-only: the browser never talks to Supabase (photos go by email, see
# below), so no anon/publishable key is needed here.
NEXT_PUBLIC_SUPABASE_URL=
# The SECRET key (sb_secret_... - or the legacy service_role JWT). NOT the
# publishable/anon key: that one is blocked by row-level security and every
# quote request fails with "[ref: 42501]".
SUPABASE_SERVICE_ROLE_KEY=

# Who the "new quote request" email (photos attached) goes to.
LEADS_NOTIFICATION_EMAIL=info@cleano.services

# How that email is sent. Set EITHER of the two below - Resend is used when
# both are present. Nothing else in the site sends email.
#
# Resend (resend.com/api-keys). cleano.services is already verified there:
# DKIM at resend._domainkey.cleano.services signs as d=cleano.services and
# the return path is send.cleano.services (EU region), so mail From:
# leads@cleano.services passes SPF and DKIM. RESEND_FROM only needs setting
# to send as something other than "Cleano Website <leads@cleano.services>",
# and whatever it is must be on a domain verified in the Resend dashboard.
RESEND_API_KEY=
RESEND_FROM=

# Microsoft 365 via Graph - the alternative. Sends as a real mailbox on
# Cleano's own tenant, so the message lands in its Sent Items and replies
# land in its Inbox. Needs an Azure app registration with the Mail.Send
# application permission (admin-consented); Cleano Ops uses the same one.
MICROSOFT_TENANT_ID=
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_SENDER_MAILBOX=info@cleano.services

# Protects /api/cron/purge-leads (the daily 12-month enquiry deletion,
# scheduled in vercel.json). Vercel sends it as a bearer token on each cron
# run; generate any long random string. Unset = the route refuses to run.
CRON_SECRET=

# Public contact details shown across the site (footer, sticky bar, WhatsApp
# button). Kept as env vars so they can change without a code edit.
NEXT_PUBLIC_CONTACT_PHONE=02033704040
NEXT_PUBLIC_CONTACT_PHONE_DISPLAY="020 3370 4040"
NEXT_PUBLIC_CONTACT_EMAIL=info@cleano.services
# WhatsApp deep links need the number in international format, no leading 0/+.
NEXT_PUBLIC_WHATSAPP_NUMBER=442033704040

# Optional bot protection for the quote form (Cloudflare dashboard ->
# Turnstile - same account already used for this domain's DNS). The form
# and API both work fine with these unset; adding them just makes the
# quote form require a passed Turnstile check before it's accepted.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
